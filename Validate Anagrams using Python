def anagram(word1, word2):

    if sorted(word1) == sorted(word2):
        return True
    else:
        return False

print(anagram(word1=input("Enter First Word"),word2=input("Enter Second Word")))
